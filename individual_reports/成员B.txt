一、外部支持：
  1. SLT版本目前已提交测试版本，峰值速率/多用户测试/HARQ缓存能力测试case流程符合预期，原本HARQ测试9用户正常，10用户异常问题定位完成，软件释放异常，在第一次重传来的时候harq bakbuffer释放存在一个idx释放阈值的判断，这个阈值是217，此时已经占用了210个buffer直接跳出了，导致harq没有释放把这个异常保护修改为：当前需要释放的缓存sram个数小于目前已经占用的缓存个数即可，目前最高支持30ms-R15-9用户测试；
    2. 长沙发包失败问题支持，发送与接收公用同一块内存池，原本接收ZX串接数据包处理异常，导致在处理尾+头串接数据包时会丢失头分段，导致该实体持续挂起，此时来的完整数据包就存在内存异常未释放的问题，修改解串接代码逻辑后已提交初步Debug版本至客户，等待反馈中；

二、SLSS测试进展：

  1. 自动化测试支持，未投入人力；

  2. SLSS打通版本V100_V1.0.0TRS_26003301800IT实验室测试支持，目前发现基于V1.0.9的主线版本后存在盲检缓存池持续占满的问题，目前正在收敛定位主线的哪些修改会导致该问题；

三、其他：
  1. NTN调研讨论；
  2. 低功耗复测结果讨论；
  3. 定位与自同步稳定版本代码合并；
  4. 无人机同步方案讨论；