### 1. FULLMASK平台打通及支持（99%）

  

（1）Fullmask-行标自同步验证

- a. 暂无新增

（2）校准综测版本维护

- a. 广通远驰新增综测支持：已完成；  
- b. V100_V1.1.22TR_2605282000IT版本校准综测支持：加入启动激活及快速同步模式后，校准前需去激活再以校准模式激活。1.1.22版本出现校准后无法同步问题，定位原因：1）S500未给SYNC发送同步准备请求；2）S500去激活SYNC消息晚于激活消息，导致SYNC模块激活失败。合入修改后问题解决；

（3）低功耗功能开发及验证

- a. PUE校准时间数据初步结论：复测了PUE版本不同睡眠时间适合的校准时长，校准时长与睡眠时长关系大致符合z=0.038846x+2.27（x为睡眠时长，z为校准时长），校准时间在3~40ms之间；

- b. 两PP1S间多次睡眠、无校准压测、多板子压测、高低温压测等CASE压测中，基于平台侧需求后续可以关注最长40ms的校准时长下，深睡最长可保持多久的同时帧头偏移在理论允许误差范围内； 

- c. DRX、LS、DS内容讨论，明确概念；

（4）定位功能开发及验证

- a. 定位实验室测试支持：两板卡基于GNSS同步的情况下，GAP符号上信号的参数测量结果不符合预期，测量Kalman滤波的频偏值异常，滤波结果为几千赫兹，时偏滤波结果为几十个Ts。保存一组Dump数据进行分析，需确定Dump数据是否存在溢出问题以及数据异常时Kalman滤波如何判断并舍弃异常数据；

- b. 两块&三板卡同步测试均出现后续设备接收到GNSS信号的情况导致不符合预期，后续代码上限制GNSS同步再进行调试；
- c. 堆栈问题：线上编译存在DUP堆栈不足，在定位代码中删除多余全局变量、部分全局复用内存、优化函数内较大局部变量，优化后线上编译仍存在该问题，后续考虑关闭平台深浅睡眠功能进行尝试；
  
（5）3GPP-slss同步方案验证

- a. 初步完成外场一阶段测试版本提交；

- b. 另外适配S500版本最新修改，3GPP版本需要合入行标版本关于PP1S维护的内容，目前存在同步自维护utc时间部分代码合并完毕，调试中；

### 2. FULLMASK二阶段设计实现

  

（1）定位相关行标同步实验室测试支持进行中；


（2）无人机同步路由方案首版本调测完成交付测试，进行拉远测试，测试case初步定义为成都范围内经纬度海拔1/3/9公里打桩，本周开始测试；

  
（3）传输参数自适应特性开发：PDCP的用户维护功能开发完成，正在进行分段传输参数模块部分的开发，本周人力投放至广通远驰，释放后继续该任务；
  

### 3. 支持工作（长期任务）

  

（1）FULLMASK系统集成版本迭代

  

- a. V100_V1.1.23TR_2605282000IT版本迭代支持；


（2）FULLMASK系统集成测试支持

  
- a. ALPS需支持主线版本支持SLSS行标子同步特性，调测中发现V1.0.8以后AP版本均会对SLSS寄存器进行异常初始化赋值导致SLSS功能异常，定位至节电需求默认上电后对SYNC模块下电DUP再上电，导致异常赋值，取消下电操作后AP版本可正常使用，正在集成可用版本中；

  

（3）外部版本维护

  - a. UAV版本提价测试进行拉远测试；
  - b. SLT测试讨论与版本修改，更新波形文件；

（4）外部问题定位及支持


- a. 长沙发包失败问题支持：长跑5天后再次出现链表内存未释放问题，增加定时查询链表异常占用状态功能，异常时主动释放，目前客户现场未出现异常，持续跟踪；

- b. 20HZ 4K的丢包率问题追踪，分析最新日志存在灌入GNSS的设备存在2s时的逻辑信道重选，导致引入额外的资源选择，资源选择次数增加会导致PER增加，逻辑信道重选初步定位至逻辑信道去激活时的初始化存在错误，本周发布LC占用状态正常初始化版本，已递交；

（5）自动化工具交流讨论；


### 4. 项目及资源工作（长期任务）

  

（1）暂无新增；