### 1. FULLMASK平台打通及支持（99%）

  

（1）Fullmask-行标自同步验证

   - a. 新版AP与合并后的CP代码联调出现同步失败：AP打印O_OMSYNC_UTC_UPDT消息未处理导致核间通信堵死，正在定位中；

（2）校准综测版本维护

   - a. 广通远驰新增综测支持：已完成；
   - b. V100_V1.1.22TR_2605282000IT版本校准综测支持：校准后无法同步问题已定位并解决（S500未发送SYNC同步准备请求，且去激活消息晚于激活消息导致SYNC模块激活失败），合入修改后问题解决；

（3）低功耗功能开发及验证

   - a. PUE校准时间数据初步结论：复测了PUE版本不同睡眠时间适合的校准时长，校准时长与睡眠时长关系大致符合z=0.038846x+2.27（x为睡眠时长，z为校准时长），校准时间在3~40ms之间；

   - b. 两PP1S间多次睡眠、无校准压测、多板子压测、高低温压测等CASE压测中，基于平台侧需求后续可以关注最长40ms的校准时长下，深睡最长可保持多久的同时帧头偏移在理论允许误差范围内； 
   
   - c. DRX、LS、DS方案讨论持续进行中；WFI及LS电流复测进行中；

（4）定位功能开发及验证

   - a. 定位实验室测试支持：两板卡基于GNSS同步的情况下，GAP符号上信号的参数测量结果不符合预期，测量Kalman滤波的频偏值异常，滤波结果为几千赫兹，时偏滤波结果为几十个Ts。保存一组Dump数据进行分析，需确定Dump数据是否存在溢出问题以及数据异常时Kalman滤波如何判断并舍弃异常数据；

   - b. 多板卡同步测试：板卡A基于GNSS同步、板卡B以板卡A为同步源时，当两块均为同步RSU，板卡B发送SSB和信标信号会导致无法与板卡A保持同步；当板卡A为RSU、板卡B为OBU时，板卡B仅有SSB计数无信标计数。问题跟踪中；DUP跑死原因已定位；

   - c. 堆栈问题：线上编译存在DUP堆栈不足，在定位代码中删除多余全局变量、部分全局复用内存、优化函数内较大局部变量，优化后线上编译仍存在该问题，后续考虑关闭平台深浅睡眠功能进行尝试；
  
（5）3GPP-slss同步方案验证

   - a. 初步完成外场一阶段测试版本提交；

   - b. 适配S500版本最新修改，3GPP版本合入行标版本关于PP1S维护的内容，同步自维护UTC时间部分代码合并完毕，调试中；

### 2. FULLMASK二阶段设计实现

  

（1）定位相关行标同步实验室测试支持进行中；


（2）无人机同步路由方案
   
   - a. 信道模拟器拉远测试：1公里测试整体结果符合预期，量测设备均可自动调节TA发送提前量；3/5/9公里均存在USER侧设备的业务丢包，目前正在分析中；
  

（3）传输参数自适应特性开发
   
   - a. 总体进度80%。已完成：PDCP接收端对接收用户的维护、PDCP发送端对超时用户的清理、传输方式1/2/3、小包处理、传输方式2下的MCS调整；未完成：进程选择流程更新；
  

### 3. 支持工作（长期任务）

  

（1）FULLMASK系统集成版本迭代

  

   - a. V100_V1.1.24TR_2605282000IT版本发布，支持CBR上报问题修改，已提交；


（2）FULLMASK系统集成测试支持

  
   - a. ALPS需支持主线版本支持SLSS行标子同步特性，调测中发现V1.0.8以后AP版本均会对SLSS寄存器进行异常初始化赋值导致SLSS功能异常，定位至节电需求默认上电后对SYNC模块下电DUP再上电，导致异常赋值，取消下电操作后AP版本可正常使用，正在集成可用版本中；

  

（3）外部版本维护

   - a. UAV版本拉远测试进行中；
   - b. SLT测试支持与波形文件更新；

（4）外部问题定位及支持


   - a. 长沙发包失败问题支持：长跑5天后再次出现链表内存未释放问题，增加定时查询链表异常占用状态功能，异常时主动释放，目前客户现场未出现异常，持续跟踪；

   - b. 20HZ 4K的丢包率问题追踪：定位至逻辑信道去激活时初始化存在错误导致2s时异常重选，增加额外资源选择次数导致PER上升；已发布LC占用状态正常初始化版本并递交；

（5）自动化工具交流讨论；


### 4. 项目及资源工作（长期任务）

  

（1）暂无新增；